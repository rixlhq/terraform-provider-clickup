---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "clickup_task Resource - terraform-provider-clickup"
subcategory: ""
description: |-
  
---

# clickup_task (Resource)





<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `list_id` (String) ClickUp list_id path parameter required to create this task.
- `name` (String)

### Optional

- `archived` (Boolean)
- `assignees` (Attributes) (see [below for nested schema](#nestedatt--assignees))
- `check_required_custom_fields` (Boolean) When creating a task via API any required Custom Fields are ignored by default (`false`).\
 \
You can enforce required Custom Fields by including `check_required_custom_fields: true`.
- `custom_fields` (Attributes List) You can include one or more Custom Fields to set them when creating a new task.\
 \
Custom Fields that use object and array type values are nullable by sending `"value": null`.\
 \
Only Custom Fields applicable to the task's `custom_item_id` are saved. Values for fields scoped to other custom task types are ignored. Use a [Get Custom Fields](ref:getaccessiblecustomfields) endpoint and check each field's `applied_objects` to see which fields apply to which task types before creating the task. (see [below for nested schema](#nestedatt--custom_fields))
- `custom_item_id` (Number) The custom task type ID for this task. A value of `0` (default) creates a standard task type "Task".\
 \
This determines which Custom Fields from the `custom_fields` array are accepted.\
 \
To get a list of available custom task type IDs for your Workspace, use the [Get Custom Task Types endpoint](ref:getcustomitems).
- `custom_task_ids` (Boolean) If you want to reference a task by its custom task id, this value must be `true`.
- `description` (String)
- `due_date` (Number)
- `due_date_time` (Boolean)
- `group_assignees` (Attributes) (see [below for nested schema](#nestedatt--group_assignees))
- `include_markdown_description` (Boolean) To return task descriptions in Markdown format, use `?include_markdown_description=true`.
- `include_subtasks` (Boolean) Include subtasks, default false
- `links_to` (String) Include a task ID to create a linked dependency with your new task.
- `markdown_content` (String) Markdown formatted description for the task. If both `markdown_content` and `description` are provided, `markdown_content` will be used instead of `description`.
- `notify_all` (Boolean) If `notify_all` is true, the creator of the task will also be notified. Other assignees and watchers are always notified regardless of this setting.
- `parent` (String) You can create a subtask by including an existing task ID.\
 \
The `parent` task ID you include can be a subtask, but must be in the same List specified in the path parameter.
- `points` (Number) Add Sprint Points to the task.
- `priority` (Number)
- `start_date` (Number)
- `start_date_time` (Boolean)
- `status` (String)
- `tags` (List of String)
- `task_id` (String)
- `team_id` (Number) When the `custom_task_ids` parameter is set to `true`, the Workspace ID must be provided using the `team_id` parameter.
 \
For example: `custom_task_ids=true&team_id=123`.
- `time_estimate` (Number)
- `watchers` (Attributes) (see [below for nested schema](#nestedatt--watchers))

<a id="nestedatt--assignees"></a>
### Nested Schema for `assignees`

Optional:

- `add` (List of Number)
- `rem` (List of Number)


<a id="nestedatt--custom_fields"></a>
### Nested Schema for `custom_fields`

Required:

- `id` (String)
- `value` (String) JSON-encoded custom field value. Use jsonencode() for non-string values.


<a id="nestedatt--group_assignees"></a>
### Nested Schema for `group_assignees`

Optional:

- `add` (List of String)
- `rem` (List of String)


<a id="nestedatt--watchers"></a>
### Nested Schema for `watchers`

Optional:

- `add` (List of Number)
- `rem` (List of Number)
